# Evaluations

This folder stores small, repeatable scenarios for tracking report quality over time.

## Suggested Workflow

1. Add representative research prompts to `scenarios/`.
2. Run the backend with a seeded document set.
3. Compare evaluation scores such as citation density and evidence coverage after code changes.

The bundled scenarios are intentionally lightweight so the starter repository works without proprietary data.

